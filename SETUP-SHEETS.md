# 답변 수집 설정 (구글 시트)

봉사자가 사이트에서 "정해진 내용"을 적어 보내면 구글 시트에 쌓이도록 하는 설정입니다. 한 번만 해두면 됩니다.

이 설정은 **구글 계정으로 로그인해서 직접** 해야 합니다. 아래 순서대로 따라가시면 됩니다.

## 1. 시트 만들기

1. [sheets.new](https://sheets.new) 로 새 스프레드시트를 만듭니다.
2. 이름을 알아보기 쉽게 바꿉니다. 예) `바자회 답변 수집`

시트 안에 직접 적을 것은 없습니다. 첫 답변이 들어오면 「답변」 시트와 머리글이 자동으로 만들어집니다.

## 2. 스크립트 붙여넣기

1. 시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 를 엽니다.
2. 편집기에 있던 내용을 모두 지웁니다.
3. 이 저장소의 [`assets/answers.gs`](assets/answers.gs) 내용을 전부 복사해 붙여넣습니다.
4. 맨 위의 이 줄을 찾아 암호를 정합니다.

   ```
   var PASSWORD = '여기에-공유-암호를-적으세요';
   ```

   봉사자들에게 알려줄 암호입니다. 따옴표 안만 바꾸세요. 어려운 암호일 필요는 없지만, 교회 이름처럼 쉽게 짐작되는 것은 피하세요.
5. 저장합니다 (Ctrl+S).

## 3. 웹 앱으로 배포하기

1. 오른쪽 위 **배포 → 새 배포** 를 누릅니다.
2. 톱니바퀴(유형 선택)에서 **웹 앱** 을 고릅니다.
3. 설정을 이렇게 맞춥니다.

   | 항목 | 값 |
   |---|---|
   | 실행 계정 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

   「모든 사용자」로 두어야 로그인 없이 입력할 수 있습니다. 대신 주소를 아는 사람은 누구나 요청을 보낼 수 있으므로, 2번에서 정한 암호가 실제 잠금장치 역할을 합니다.
4. **배포** 를 누르고 권한을 허용합니다. 처음에는 "이 앱은 확인되지 않았습니다" 경고가 나오는데, **고급 → (프로젝트 이름)(으)로 이동** 을 눌러 진행하면 됩니다. 본인이 만든 스크립트이므로 정상입니다.
5. 마지막에 나오는 **웹 앱 URL** 을 복사합니다. `https://script.google.com/macros/s/...../exec` 형태입니다.

## 4. 사이트에 연결하기

[`assets/config.js`](assets/config.js) 의 큰따옴표 안에 3번에서 복사한 주소를 넣고 커밋합니다.

```js
window.ANSWERS_URL = "https://script.google.com/macros/s/AKfy...../exec";
```

비워두면 입력 페이지가 "아직 연결되지 않았습니다" 안내만 보여주고 입력을 받지 않습니다.

## 5. 확인하기

사이트에서 **정해진 내용 알려주기** 페이지를 열고, 이름과 암호를 넣어 한 건 보내봅니다. 시트에 줄이 하나 늘어나면 됩니다.

---

## 알아두실 점

- **배포 주소는 공개됩니다.** 사이트가 공개 저장소이므로 주소를 숨길 수 없습니다. 암호가 실제 잠금장치입니다.
- **암호는 사이트 코드에 들어가지 않습니다.** 스크립트 쪽에서만 확인하므로, 소스를 봐도 암호는 알 수 없습니다.
- **암호를 바꾸려면** 2번의 `PASSWORD` 를 고치고 **배포 → 배포 관리 → 수정** 으로 새 버전을 배포하면 됩니다. 주소는 그대로입니다.
- **스크립트를 고친 뒤에는 반드시 새 버전을 배포해야** 반영됩니다. 저장만 해서는 바뀌지 않습니다.
- **답변은 문서가 아닙니다.** 시트에 쌓인 내용은 "이렇게 정했다"는 보고이고, 확정된 것만 `docs/`의 문서에 옮겨 적습니다. 문서가 계속 단일 기준으로 남아야 인쇄본과 어긋나지 않습니다.
- 시트는 기본적으로 만든 사람만 볼 수 있습니다. 다른 임원과 함께 보려면 시트 자체를 공유하세요.
