# 답변 수집 설정 (구글 시트)

봉사자가 사이트에서 "정해진 내용"을 적어 보내면 구글 시트에 쌓이도록 하는 설정입니다. 한 번만 해두면 됩니다.

이 설정은 **구글 계정으로 로그인해서 직접** 해야 합니다. 아래 순서대로 따라가시면 됩니다.

## 시작 전에 — 계정 확인

두 가지를 먼저 확인하세요. 여기서 막히면 3번 배포 단계에서 **"이 작업을 수행할 권한이 없습니다"** 로 실패합니다.

- **시트를 본인이 소유한 계정으로 로그인**되어 있어야 합니다. 남에게 공유받은 시트는 스크립트를 붙여넣고 저장까지는 되지만 **배포가 되지 않습니다.** 공유받은 시트라면 아래 1번대로 본인 계정에서 시트를 새로 만드세요.
- **회사·학교 계정(Workspace)은 피하는 편이 안전합니다.** 관리자 정책으로 Apps Script 배포나 「모든 사용자」 공개가 막혀 있는 경우가 많습니다. 계정 메뉴에 "○○○.com에서 관리" 라고 적혀 있으면 조직 계정입니다. 개인 구글 계정을 쓰면 이 문제가 없고, 나중에 직장이 바뀌어도 자료가 남습니다.

## 1. 시트 만들기

1. [sheets.new](https://sheets.new) 로 새 스프레드시트를 만듭니다.
2. 이름을 알아보기 쉽게 바꿉니다. 예) `바자회 답변 수집`

시트 안에 직접 적을 것은 없습니다. 첫 답변이 들어오면 「답변」 시트와 머리글이 자동으로 만들어집니다.

## 2. 스크립트 붙여넣기

1. 시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 를 엽니다.
2. 편집기에 있던 내용을 모두 지웁니다.
3. 이 저장소의 [`assets/answers.gs`](assets/answers.gs) 내용을 전부 복사해 붙여넣습니다.
4. 맨 위의 이 줄을 찾아 암호를 정합니다.

   ```
   var PASSWORD = '여기에-공유-암호를-적으세요';
   ```

   봉사자들에게 알려줄 암호입니다. 따옴표 안만 바꾸세요. 어려운 암호일 필요는 없지만, 교회 이름처럼 쉽게 짐작되는 것은 피하세요.
5. 저장합니다 (Ctrl+S).

## 3. 웹 앱으로 배포하기

1. 오른쪽 위 **배포 → 새 배포** 를 누릅니다.
2. 톱니바퀴(유형 선택)에서 **웹 앱** 을 고릅니다.
3. 설정을 이렇게 맞춥니다.

   | 항목 | 값 |
   |---|---|
   | 실행 계정 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

   「모든 사용자」로 두어야 로그인 없이 입력할 수 있습니다. 대신 주소를 아는 사람은 누구나 요청을 보낼 수 있으므로, 2번에서 정한 암호가 실제 잠금장치 역할을 합니다.
4. **배포** 를 누르고 권한을 허용합니다. 처음에는 "이 앱은 확인되지 않았습니다" 경고가 나오는데, **고급 → (프로젝트 이름)(으)로 이동** 을 눌러 진행하면 됩니다. 본인이 만든 스크립트이므로 정상입니다.
5. 마지막에 나오는 **웹 앱 URL** 을 복사합니다. `https://script.google.com/macros/s/...../exec` 형태입니다.

## 4. 사이트에 연결하기

[`assets/config.js`](assets/config.js) 의 큰따옴표 안에 3번에서 복사한 주소를 넣고 커밋합니다.

```js
window.ANSWERS_URL = "https://script.google.com/macros/s/AKfy...../exec";
```

비워두면 입력 페이지가 "아직 연결되지 않았습니다" 안내만 보여주고 입력을 받지 않습니다.

## 5. 확인하기

사이트에서 **정해진 내용 알려주기** 페이지를 열고, 이름과 암호를 넣어 한 건 보내봅니다. 시트에 줄이 하나 늘어나면 됩니다.

## 6. 정산 도구도 같은 주소를 씁니다

[교환권 정산 도구](voucher-settlement/index.html)의 **[4 저장·백업]** 탭에서 「시트로 보내기」를 누르면, 위와 **같은 스크립트·같은 시트**에 정산 내용이 올라갑니다. 따로 설정할 것은 없고, 위 1~4번을 마쳤으면 바로 됩니다.

탭 세 개가 자동으로 만들어집니다.

| 탭 | 무엇이 들어가나 |
|---|---|
| **정산** | 사람이 읽는 표. 조별 매출, 교환소 대사, 최종 수익금 |
| **정산_계산** | **시트 함수가 들어 있는 계산기.** 노란 칸에 값을 넣으면 대사와 수익금이 시트 안에서 다시 계산됩니다 |
| **정산_원본** | 도구가 다시 불러오기 위한 기록. 사람이 읽을 것은 아니니 **지우지 마세요** |

### 「정산」과 「정산_계산」의 차이

| | 정산 | 정산_계산 |
|---|---|---|
| 성격 | 결과를 찍어 둔 **사진** | 살아 있는 **계산기** |
| 숫자를 고치면 | 아무것도 따라 변하지 않음 | **다시 계산됨** |
| 쓸 때 | 결과를 보고·보관할 때 | **준비 사이트 없이** 값만 넣어 정산할 때 |

「정산_계산」은 이 사이트가 없어져도 혼자 동작합니다. 사이트의 계산식을 그대로 시트 함수로 옮겨 적었기 때문입니다. 스프레드시트 상단의 **「바자회 정산」 메뉴 → 「빈 계산 시트 만들기」**로 빈 양식만 새로 만들 수도 있습니다 (메뉴는 시트를 새로 고쳐야 나타납니다).

> **「정산_계산」에 직접 입력하실 거면 탭을 복사해서 쓰세요.** 도구에서 「보내기」를 누르면 이 탭도 통째로 새로 만들어져 손으로 적은 것이 사라집니다.

> **보낼 때마다 세 탭을 통째로 덮어씁니다.** 정산은 한 사람이 한 자리에서 하는 일이라, 이력을 쌓기보다 「지금 도구에 있는 내용과 시트가 같다」가 중요하기 때문입니다. 여러 사람이 나눠 입력하면 **나중에 보낸 쪽이 앞의 것을 덮어씁니다.**

**이미 배포를 해두셨다면** `assets/answers.gs`를 다시 붙여넣고 **배포 → 배포 관리 → 수정**으로 새 버전을 배포해야 합니다. 정산 기능과 계산 시트가 추가되었기 때문입니다. 주소는 그대로입니다.

---

## 알아두실 점

- **배포 주소는 공개됩니다.** 사이트가 공개 저장소이므로 주소를 숨길 수 없습니다. 암호가 실제 잠금장치입니다.
- **암호는 사이트 코드에 들어가지 않습니다.** 스크립트 쪽에서만 확인하므로, 소스를 봐도 암호는 알 수 없습니다.
- **암호를 바꾸려면** 2번의 `PASSWORD` 를 고치고 **배포 → 배포 관리 → 수정** 으로 새 버전을 배포하면 됩니다. 주소는 그대로입니다.
- **스크립트를 고친 뒤에는 반드시 새 버전을 배포해야** 반영됩니다. 저장만 해서는 바뀌지 않습니다.
- **답변은 문서가 아닙니다.** 시트에 쌓인 내용은 "이렇게 정했다"는 보고이고, 확정된 것만 `docs/`의 문서에 옮겨 적습니다. 문서가 계속 단일 기준으로 남아야 인쇄본과 어긋나지 않습니다.
- 시트는 기본적으로 만든 사람만 볼 수 있습니다. 다른 임원과 함께 보려면 시트 자체를 공유하세요. **공유는 배포를 끝낸 뒤에 하세요** — 공유받은 사람은 배포를 할 수 없습니다.
